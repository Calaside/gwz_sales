SELECT
    date_date,
    ROUND(SUM(turnover), 2) AS daily_turnover,
    ROUND(SUM(purchase_cost), 2) AS daily_purchase_cost,
    ROUND(SUM(turnover - turnover_before_promo), 2) AS total_promo
FROM FROM `my-first-project-426411.course17.gwz_sales`
GROUP BY
    date_date
